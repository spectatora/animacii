[project]
name = "epg-pipeline"
version = "0.1.0"
description = "EPG pipeline за детски ТВ канали: сваляне на XMLTV, нормализация на заглавия, групиране на предавания"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
